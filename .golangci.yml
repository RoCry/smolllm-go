version: "2"

run:
  go: "1.26"
  timeout: 5m
  modules-download-mode: readonly
  tests: true

output:
  formats:
    colored-line-number: {}

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

linters:
  disable:
    - depguard
  enable:
    - errorlint
    - goconst
    - gocritic
    - lll
    - misspell
    - sloglint
    - nolintlint
    - paralleltest
    - testifylint
    - exhaustruct
    - errcheck
    - govet
    - ineffassign
    - revive
    - staticcheck

formatters:
  enable:
    - gofmt
    - goimports

linters-settings:
  errcheck:
    check-type-assertions: true
    check-blank: true
  govet:
    enable:
      - shadow
  gofmt:
    simplify: true
  goimports:
    local-prefixes: github.com/rocry/smolllm-go
  revive:
    severity: error
    rules:
      - name: exported
        severity: error
      - name: indent-error-flow
      - name: errorf
      - name: var-naming
      - name: receiver-naming
      - name: empty-lines
  staticcheck:
    checks:
      - all
  gosimple:
    checks:
      - all
  lll:
    line-length: 140
